# Job Applications Utility

Reusable local data and form mappings for completing varied job applications efficiently.

Copy `profile.example.json` to `profile.json` and fill it with personal data. Personal profile data, resumes, cover letters, and secrets are ignored by Git.

Commands: `python application_utility.py validate-profile` and `python application_utility.py ashby-fields`.
